package main

// RoleBindingTable ²print the rbac rolebinding
func RoleBindingTable(rolebinding RoleBindings, config *Config) {
	//data := [][]string{}
	//for i := 0; i < len(rolebinding.RoleBindings); i++ {
	//	if InArray(rolebinding.RoleBindings[i].Metadata.Name, config.File.ClusterRoleBindings.Exclude) {
	//		if CheckEnvVarLog() {
	//			fmt.Println("Value : " + rolebinding.RoleBindings[i].Metadata.Name + " excluded")
	//		}
	//	} else {
	//		fmt.Println(rolebinding.RoleBindings[i].Metadata.Name)
	//	}
	//}

}
